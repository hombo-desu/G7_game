using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 共有確認 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*===========================-
         * 
         * 泰真参上!!!!!!!!
         * お宝はいただいていくぜ、とっつぁん
         * んがあああああああああ
         * 
         * =============================*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
